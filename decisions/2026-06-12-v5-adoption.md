---
id: adr-006-stateofus-v5-adoption
title: "v5 design adopted as build source; share-landing from spec; admin auth shipped as the ADMIN_TOKEN cookie gate (adr-002; Authentik wrap deferred); all 105 questions approved"
date: 2026-06-12
status: accepted
tags: [stage-3, design, curated]
linked_features: [feat-share-landing, feat-admin-console, feat-seed-content-pipeline]
---

> **Amendment (2026-06-30, Ottomate deep-dive):** Decision §3 below ("Admin auth =
> Authentik forward-auth at the NPM proxy") did NOT ship. The shipped admin gate is the
> **app-level ADMIN_TOKEN → signed httpOnly cookie middleware** already prescribed by
> adr-002 and CODING_GUIDELINES #8. The Authentik forward-auth wrap was deferred (the
> bootstrap token expired; see iteration-log) and was never wired — there is no Authentik
> outpost in docker-compose. The `auth_strategy != none` deploy-gate condition is satisfied
> by the ADMIN_TOKEN gate. Treat §3 and the §Consequences NPM-proxy line as superseded.

## Context

v5 prototype (State of Us v5.dc.html) landed from the design agent: all 4 tranches
built (T1 brand/copy/fixes, T2 Lab-B winners, T3 interactions incl. sorter
tray+solid-buckets+early-submit and rank drag, T4 content with 24 reference
questions covering every mode and DV family). Round-1 owner answers locked:
tagline "You answer. India answers back.", six desks incl. The Mirror (lavender
#B79CE4), light-only, logo file, 5 heroes at 9s, house rules reframed privacy-first.
Known deferred to build pass: CA-009 global mono type floor (>=10px).

Four owner decisions taken 2026-06-12 (this session) to unblock Stage 3:

## Decisions

1. **v5 prototype is the build source of truth.** Preserved at design/v5/.
   Where V5 Lab Feedback interim picks conflict with the built prototype
   (N2/AR2/RG2/M2 in build vs N3/AR1/RG1/M1 in the agent pick list), the BUILT
   v5 prototype wins.
2. **feat-share-landing has no v5 design** (gap found in session-2 audit, never
   relayed to design agent). Owner: coding agent designs it from the SO1
   flow-inbound-share spec in v5 visual language; owner reviews the built page.
3. **Admin auth = Authentik forward-auth at the NPM proxy on /admin routes**
   (house pattern, as LeapLoop). Public surface stays anonymous; no app-level
   account system; auth_strategy != none satisfied for the deploy gate.
   <!-- SUPERSEDED 2026-06-30 (see top amendment): shipped as the ADMIN_TOKEN
   signed-cookie middleware gate (adr-002), not Authentik. -->
4. **Launch content approved: all 105 questions** (80 authored v1 + 25
   catalogue imports) seeded ACTIVE. Owner review AC on
   feat-seed-content-pipeline satisfied by explicit sign-off 2026-06-12.
   Pause/edit later via admin console.
5. **Scope: all 17 features per adr-003 verbatim** — no P1 deferrals.

## Consequences

- Stage 3 codegen ports v5 renderers/interactions; CA-009 type floor applied
  during build.
- Share-landing page gets a verifier pass + explicit owner review flag.
- NPM proxy host config gains a forward-auth location block before deploy gate.
